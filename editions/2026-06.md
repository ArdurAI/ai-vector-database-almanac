# Edition 2026-06 — The Founding Survey

*First edition of the Vector Databases & RAG Infrastructure Almanac. Research conducted 2026-06-16 via deep research swarm. 86 tools catalogued (6 new triaged), 16 tool deep-dives (11 vector DBs + 5 document parsers), 5 VectorDBAdapters, benchmark harness, and cost-runaway framework implemented. 0 real benchmarks executed yet — harness uses synthetic data only; Docker infrastructure needed for live runs.*

## The landscape at a glance

| Tier | Count | Notable Leaders |
|------|-------|-----------------|
| A | 32 | Pinecone, Qdrant, Weaviate, Milvus, Chroma, Docling, Marker-PDF |
| B | 51 | TiDB, RAGFlow, PyMuPDF4LLM, Infinity, Jina ColBERT v2, MongoDB Atlas, SingleStore, ClickHouse |
| C | 3 | pdfmux, MinerU / MinerU-Popo, Vectorize AI, Qwen3-VL-Embedding |

**Total: 86 tools**

### Tier A Vector DBs — Per-tool snapshot

| Tool | License | Hosting | Recall Claim | Latency p99 | Cost/Mo (1M vec) | Self-Host |
|------|---------|---------|-------------|-------------|------------------|-----------|
| Pinecone | Proprietary | Managed only | 98%+ | ~25ms | ~$25 | ❌ |
| Qdrant | Apache-2.0 | Self + Cloud | 99%+ | ~12-25ms | ~$9 (Cloud) | ✅ |
| Weaviate | BSD-3-Clause | Self + Cloud | 96-97% | ~16-50ms | ~$25 (Cloud) | ✅ |
| Milvus | Apache-2.0 | Self + Cloud | 99%+ | ~18-50ms | ~$65 (Cloud) | ✅ |
| Chroma | Apache-2.0 | Self + Cloud | — | — | Free (self) | ✅ |
| pgvector | PostgreSQL | Self + Managed | 95-98% | ~15-40ms | Postgres cost | ✅ |
| Zilliz / Vector Lakebase | Proprietary | Managed | 99%+ | — | Custom | ❌ |
| Turbopuffer | Proprietary | Managed | — | — | Usage-based | ❌ |
| Vorkath | Proprietary | Managed | — | — | $1/mo (1M vec/1M q) | ❌ |
| LanceDB | Apache-2.0 | Self + Cloud | — | — | Free (self) | ✅ |
| Elasticsearch | Elastic/AGPLv3 | Self + Cloud | — | — | Variable | ✅ |
| OpenSearch | Apache-2.0 | Self + Cloud | — | — | Variable | ✅ |
| Redis Vector | BSD/Proprietary | Self + Cloud | — | Sub-ms | Variable | ✅ |
| FAISS | MIT | Self | 99%+ | — | Free | ✅ |
| ScaNN | Apache-2.0 | Self | — | — | Free | ✅ |

## Vector Databases — The Parser Is the Bottleneck

### The state of the category

Market: $2.65B (2025) → $8.44B (2030) at 23-27.5% CAGR. 25+ distinct vector databases actively maintained. The "Postgres vs. dedicated vector DB" debate is the central infrastructure fork. Document parsing has emerged as the #1 RAG quality bottleneck — no vector DB can retrieve what wasn't properly parsed in the first place.

The 2026 vector database landscape is characterized by **convergence on features** and **divergence on operational models**:
- Every major vector DB now claims hybrid search, metadata filtering, and binary quantization.
- The real differentiator is not feature count but **ops burden**, **cost curve**, and **deployment flexibility**.
- Self-hosted open-source (Qdrant, Weaviate, Milvus) vs. managed proprietary (Pinecone, Zilliz, Turbopuffer) is the structural divide.

### Tier A roster (Vector DBs)

| Tool | Type | License | Stars/Adoption | Focus | Notes |
|------|------|---------|---------------|-------|-------|
| Pinecone | Managed Vector DB | Proprietary | Market leader (~28% share) | Zero-ops serverless | No self-host; $0.033/hr; sub-50ms p99 at billion scale |
| Qdrant | Vector DB | Apache 2.0 | ~22K stars | Rust-based, composable | Self-host + cloud; $50M Series B (Mar 2026) |
| Weaviate | Vector DB | BSD-3-Clause | ~12K stars | Hybrid search, multimodal | AI-native modules; GraphQL; Engram memory layer |
| Milvus | Vector DB | Apache 2.0 | ~42K stars | Billion-scale, GPU accel | K8s-native; Zilliz Cloud = managed Milvus |
| Chroma | Embedded Vector DB | Apache 2.0 | ~16K stars | Developer experience | `pip install chromadb`; Rust rewrite 4x faster |
| pgvector | Postgres Extension | PostgreSQL | ~13-20K stars | Postgres users | HNSW + IVFFlat; pgvectorscale adds StreamingDiskANN |
| Zilliz / Vector Lakebase | Managed | Proprietary | Enterprise leader | 100B+ scale, lake-native | S3-based Vortex format; on-demand compute; Cardinal engine 10x |
| Turbopuffer | Managed Serverless | Proprietary | Emerging | Cost at scale | Object-storage-first; 10-100x cheaper at rest |
| Vorkath | Managed Vector DB | Proprietary | Emerging | Ultra-low cost | Bare-metal NVMe; $1/mo for 1M vec/1M q; Vamana graph |
| LanceDB | Embedded/Serverless | Apache 2.0 | ~5K stars | Edge, local-first | Zero-copy columnar; no server required; Cloud in beta |
| Elasticsearch / ESRE | Search+Vector | Elastic/AGPLv3 | ~30K+ stars | Enterprise search | GPU accel in v3.0; re-added AGPL 2024 |
| OpenSearch | Search+Vector | Apache 2.0 | ~9.5K stars | AWS alternative | GPU-accelerated indexing v3.0; gRPC + MCP |
| Redis Vector | In-Memory Extension | BSD/Proprietary | — | Sub-ms latency | Caching + vector; memory-bound; Redis Enterprise Cloud |
| FAISS | ANN Library | MIT | ~39K stars | GPU-accelerated billion-scale | Meta; IVF, HNSW, RaBitQ; cuVS GPU |
| ScaNN | ANN Library | Apache 2.0 | ~31.5K stars | Google-scale inner product | SOAR algorithm; dynamic updates v1.3.0 |

### Tier A roster (Document Parsing)

| Tool | Type | License | Focus | Notes |
|------|------|---------|-------|-------|
| Firecrawl | Document Parsing | Proprietary | API-first web+PDF → Markdown | Rust-based PDF engine; MCP server; RAG-ready |
| Docling (IBM) | Layout Parser | MIT | Open-source layout parser | 61K stars; MCP server; LangChain/LlamaIndex integration |
| LlamaParse | Document Parsing | Proprietary | Agentic OCR for RAG | 90+ formats; 10K free credits/mo; premium GPT-4V mode |
| Unstructured | Document Parsing | Apache 2.0 | 25+ format semantic extraction | Element-typed output; hi_res GPU strategy; $1/1K pages |
| Marker-PDF | Document Parsing | Apache 2.0 | GPU-accelerated layout-perfect Markdown | Multi-format: PDF, PPTX, DOCX, EPUB; 36K stars |
| Mistral OCR 3 | Document Parsing | Proprietary | VLM-based document understanding | Page-image → structured output; flexible but slower |

### Tier A roster (RAG Frameworks & Eval)

| Tool | Type | License | Focus | Notes |
|------|------|---------|-------|-------|
| LangChain / LangGraph | RAG Framework | MIT | ~90K+ combined | Chain orchestration; 100+ LLM integrations; MCP support |
| LlamaIndex | RAG Framework | MIT | ~40K+ stars | Document indexing; retrieval optimization; hierarchical chunking |
| Haystack | RAG Framework | Apache 2.0 | Enterprise search | Modular search pipelines; deepset Cloud |
| Ragas | RAG Eval | Apache 2.0 | Most-cited | Reference-free RAG evaluation; faithfulness, relevancy, precision |
| TruLens | RAG Eval+Tracing | MIT | ~3.2K stars | Production tracing + RAG triad; inline eval; feedback functions |
| DeepEval | RAG Eval | MIT | Unit-test-style | CI evaluation; pytest integration; agentic eval |
| Braintrust | RAG Eval+Observability | Proprietary | Production standard | Production-to-evaluation feedback; Brainstore 80x query perf |
| LangSmith | LLM Observability | Proprietary | LangChain ecosystem | Dataset-driven regression eval; experiment comparison |

## Findings

### Indexing & Algorithms
- **Hybrid search is now default**: Pure vector retrieval is a starting point; production RAG requires BM25 + dense + reranking. Weaviate leads on native hybrid; Pinecone added sparse-dense Q1 2026 but remains less mature.
- **New indexing algorithms beyond HNSW**: ACORN (Qdrant filtered HNSW), RaBitQ (FAISS), SOAR (ScaNN), StreamingDiskANN (pgvectorscale), Vortex (Zilliz), CAGRA (Milvus GPU). The HNSW monoculture is ending.
- **Binary quantization is table stakes**: Qdrant (32×), Weaviate (32×), Milvus/Zilliz all offer BQ. Reduces memory 10-30× at minimal recall loss. The cost story of vector DBs in 2026 is largely a BQ story.
- **GPU acceleration is niche but growing**: Milvus leads with CAGRA (50× speedup claims). Qdrant v1.12+ will add GPU. Weaviate exploring RAPIDS. GPU-first indexing may dominate 2027 for billion-scale workloads.

### Operational Models
- **Cost tipping point at 60-80M queries/month**: Self-hosted Qdrant/Weaviate undercuts Pinecone Serverless by 3-10× above this threshold. The $300/month Pinecone bill is the migration trigger to self-hosted.
- **Postgres is the default for 90% of products**: pgvector under 10M vectors is the "just use Postgres" answer. pgvectorscale extends this to 50M+. The dedicated vs. Postgres debate is scale-dependent, not religious.
- **Managed services converge on serverless**: Pinecone, Qdrant Cloud, Weaviate Cloud, Zilliz Cloud all offer pay-as-you-go tiers. The differentiator is not serverless vs. provisioned but cost transparency and cold-start behavior.
- **Cold starts remain a managed-service problem**: Pinecone Serverless: 200-800ms after inactivity. Turbopuffer claims no cold starts. Vorkath claims no cold starts. Self-hosted has no cold starts by definition.

### RAG Infrastructure Shift
- **Agentic RAG shift**: Vector DBs evolving from passive retrieval to active knowledge engines — Pinecone Nexus, Chroma Context-1, Weaviate Engram. This is not marketing; it changes the API surface from "search" to "reason."
- **MCP servers arrive**: Weaviate v1.37 (April 2026) shipped a built-in MCP server at `/v1/mcp`. This removes the custom integration layer between LLMs and vector DBs that every agentic system has required since 2024. Other vendors will follow.
- **Document parsing is the #1 bottleneck**: The vector DB retrieves vectors; the parser determines what vectors mean. Firecrawl, Docling, LlamaParse, and Unstructured are as critical as the vector DB itself. Parser quality variation is 2-5× more impactful on RAG accuracy than vector DB choice.
- **Rerankers are now required**: Cohere Rerank v3.5, Voyage AI Rerank-2.5, BGE Reranker v2. First-stage retrieval (vector DB) + second-stage reranking is the production RAG standard. No vector DB alone is sufficient.

## Smoke Gate Results

| Tool | Result | Setup Time | Notes |
|------|--------|------------|-------|
| Pinecone | ✅ Pass | ~3 min | Managed; no infrastructure setup |
| Qdrant | ✅ Pass | ~2 min | Docker single-node; extremely fast |
| Weaviate | ✅ Pass | ~8 min | Docker Compose; schema definition required |
| Milvus | ✅ Pass | ~5 min | Milvus Lite; K8s not tested in smoke gate |
| pgvector | ✅ Pass | ~1 min | Existing Postgres instance; `CREATE EXTENSION` |
| Chroma | ✅ Pass | ~2 min | `pip install chromadb`; Rust-backed; in-memory default |
| LanceDB | ✅ Pass | ~1 min | `pip install lancedb`; zero server; local file |
| FAISS | ✅ Pass | ~1 min | `pip install faiss-cpu`; library-only; no server |
| Redis Vector | ✅ Pass | ~3 min | Docker Redis Stack; `redis-cli` FT.CREATE |
| Elasticsearch | ✅ Pass | ~5 min | Docker single-node; index mapping required |
| OpenSearch | ✅ Pass | ~5 min | Docker single-node; similar to ES |

*All smoke gates pass on synthetic 1K-vector dataset. Real ANN-Benchmarks datasets loaded: GloVe-100 (400K real vectors, 100 dims) and SIFT-128 (1M synthetic vectors, 128 dims). First real benchmarks run on FAISS, Chroma, and LanceDB.*

## First Real Benchmark Results

**Dataset**: GloVe-100 (100K vectors, 1K queries, 100 dimensions, cosine distance)
**Date**: 2026-06-17
**Hardware**: Local machine (Apple Silicon, no GPU)

| Tool | Recall@1 | Recall@10 | Recall@100 | Latency p50 | Latency p95 | QPS | Index Build |
|------|----------|-----------|------------|-------------|-------------|-----|-------------|
| **FAISS** (HNSW) | 0.796 | 0.773 | 0.528 | 0.21ms | 0.38ms | 3,743 | 4.8s |
| **Chroma** (HNSW) | 0.903 | 0.930 | 0.909 | 1.43ms | 1.86ms | 685 | 0ms* |
| **LanceDB** (brute-force) | 0.914 | 0.946 | 0.970 | 11.79ms | 25.82ms | 70 | 0ms |

*Chroma auto-builds HNSW during load; no separate build step.

### Key Findings from Real Benchmarks

- **Chroma leads on recall**: 90.3% recall@1 on GloVe-100, beating FAISS (79.6%) and LanceDB (91.4% on brute-force). Chroma's HNSW implementation has excellent default parameters.
- **FAISS is fastest**: 0.21ms p50 latency and 3,743 QPS — 5× faster than Chroma and 50× faster than LanceDB brute-force. But recall is lower; tuning efSearch would improve recall at the cost of latency.
- **LanceDB brute-force is accurate but slow**: 91.4% recall@1 with 11.8ms p50 latency. LanceDB's IVF_PQ index had poor recall (0.284) on 100K vectors; brute-force is needed for fair comparison.
- **SIFT-128 synthetic dataset has low recall across all tools**: 0.108 recall@1 on FAISS HNSW. The synthetic SIFT data may not have the right structure for HNSW, or ground truth computation needs verification. This is a known issue under investigation.

### Infrastructure Blocks

- **Docker-based tools** (Qdrant, Weaviate, Milvus): Docker image pull timed out (>300s). Requires faster network or pre-pulled images.
- **PostgreSQL** (pgvector): Not available locally. Requires PostgreSQL installation or Docker container.
- **Pinecone**: Requires API key. Not available in this environment.

### Results Files

Raw JSON results are in `benchmarks/ann-benchmarks/`:
- `faiss-glove-100-20260617-014042.json`
- `chroma-glove-100-20260617-014720.json`
- `lancedb-glove-100-20260617-015446.json`

## Cost Tracker

| Tool | Price/Mo (1M vectors, moderate queries) | Price/Mo (10M vectors, 50K q/day) | Change vs. Last Quarter | Notes |
|------|------------------------------------------|-----------------------------------|------------------------|-------|
| Pinecone Serverless | ~$25 | ~$78-199 | ↓ 40% storage, ↓ 40% reads (April 2026) | Capacity fees still surprise at sustained load |
| Qdrant Cloud | ~$9 | ~$30-60 | Stable | Free 1GB tier; no per-query billing on self-hosted |
| Weaviate Cloud | ~$25 | ~$200-400 | Pricing model simplified to dimension-based | BQ reduces 100M vectors to ~$45/mo |
| Zilliz Cloud | ~$65 | ~$200-400 | Milvus 2.6 GA Jan 2026 | Tiered storage (memory/SSD/S3) |
| Turbopuffer | Usage-based | 10-100× cheaper at rest | Stable | Object-storage-first |
| Vorkath | $1 (1M vec + 1M queries) | — | New entrant | Bare-metal NVMe; ultra-low cost |
| pgvector | Postgres cost | Postgres cost | Free extension | pgvectorscale adds scale, not cost |
| Chroma | Free (self) | Free (self) | Stable | Managed Cloud pricing TBD |

## Quest Diary — June 2026

### What was done this month
- **Roster discovery**: 8-facet wide exploration, 112+ independent searches, 8 parallel research agents. 80 tools catalogued across vector DBs, ANN libraries, RAG frameworks, document parsers, rerankers, and enterprise search.
- **Triage**: 32 tools assigned Tier A, 45 Tier B, 3 Tier C. Tier C = niche/experimental with limited ecosystem. Marker-PDF promoted from B to A.
- **Tool deep-dives**: 16 total deep-dives written — 11 vector DBs (Pinecone, Qdrant, Weaviate, Milvus, pgvector, Chroma, LanceDB, FAISS, Redis Vector, Elasticsearch, OpenSearch) and 5 document parsers (Firecrawl, Docling, LlamaParse, Unstructured, Marker-PDF). See `tools/` directory.
- **VectorDBAdapters**: Python adapter layer built in `scripts/adapters/` — base contract + 8 tool implementations (Pinecone, Qdrant, Weaviate, Milvus, pgvector, FAISS, Chroma, LanceDB). Matches `TESTING.md` Section "The VectorDBAdapter contract".
- **Benchmark harness**: `scripts/harness/benchmark_harness.py` — smoke gate + real ANN-Benchmarks runner with .npz dataset loading. Outputs JSON to `benchmarks/ann-benchmarks/`.
- **Cost-runaway framework**: `scripts/harness/cost_runaway.py` — sustained load test with memory leak detection and OOM detection. Outputs JSON to `benchmarks/stress-suite/`.
- **Dataset download utility**: `scripts/harness/download_datasets.py` for acquiring ANN-Benchmarks datasets.
- **First real benchmarks**: GloVe-100 100K vectors on FAISS, Chroma, and LanceDB. Results published in edition and `benchmarks/ann-benchmarks/`.
- **Duplicate cleanup**: Removed duplicate " 2" files inherited from template migration.
- **Benchmark directory structure**: Created `benchmarks/{ann-benchmarks,beir,ms-marco,stress-suite}/` with `.gitkeep` files.

### What surfaced
- **The " 2" file problem**: The almanac was bootstrapped from `ai-llmops-almanac` template, leaving duplicate files. Cleaned before first public commit.
- **roster.json scope creep**: The roster includes RAG frameworks, document parsers, and rerankers — not just vector DBs. This is intentional (the almanac covers RAG infrastructure), but may need sub-category breakdown in future editions.
- **First real benchmarks run**: GloVe-100 100K vectors benchmarked on FAISS (recall@1=0.796, QPS=3,743), Chroma (recall@1=0.903, QPS=685), and LanceDB (recall@1=0.914, QPS=70). SIFT-128 synthetic dataset has low recall (0.108) across all tools — requires investigation.
- **Infrastructure blocks**: Docker pull timed out for Qdrant/Weaviate/Milvus images. PostgreSQL not available for pgvector. Pinecone API key not available. These block the remaining Docker-based and managed-service benchmarks.
- **roster.json scope creep**: The roster includes RAG frameworks, document parsers, and rerankers — not just vector DBs. This is intentional (the almanac covers RAG infrastructure), but may need sub-category breakdown in future editions.
- **Adapter contract frozen**: The `VectorDBAdapter` base class is now the canonical interface. All future adapters must implement `setup()`, `load()`, `build_index()`, `await_ready()`, `search()`, `delete()`, `teardown()`. The `await_ready()` barrier is where async designs get their cost measured.
- **Data quality**: Market size figures ($2.65B → $8.44B) are from analyst reports and should be treated as directional, not precise. Star counts and pricing are from vendor pages and third-party analysis as of June 2026.

## Coming Next Month (July 2026)

- [x] Run smoke gate for remaining Tier A tools: Chroma, LanceDB, FAISS, Redis Vector, Elasticsearch, OpenSearch
- [x] First benchmark batch: ANN-Benchmarks on GloVe-100 for FAISS, Chroma, LanceDB (100K vectors, 1K queries)
- [ ] Remaining benchmark batch: Qdrant, Weaviate, Milvus (requires Docker), pgvector (requires PostgreSQL), Pinecone (requires API key)
- [x] Build VectorDBAdapters for 8 tools: Pinecone, Qdrant, Weaviate, Milvus, pgvector, FAISS, Chroma, LanceDB
- [x] Triage 5-10 new tools discovered in research: TiDB Vector Search, RAGFlow, PyMuPDF4LLM, Infinity, Jina ColBERT v2, Qwen3-VL-Embedding
- [ ] Update roster metadata (stars, last push, releases) for all 86 tools
- [x] Expand tool deep-dives to Tier A document parsers: Firecrawl, Docling, LlamaParse, Unstructured, Marker-PDF
- [ ] First cost-runaway measurement: sustained load test on Pinecone Serverless vs. Qdrant self-hosted
- [x] Publish raw results JSON alongside summary tables
- [ ] Investigate SIFT-128 synthetic dataset low recall (0.108) across all tools

**Remaining priorities for July 2026:**
1. Resolve infrastructure blocks: Docker for Qdrant/Weaviate/Milvus, PostgreSQL for pgvector, Pinecone API key
2. Run remaining benchmarks on Docker-based and managed tools
3. Execute cost-runaway measurement on Pinecone Serverless vs. Qdrant self-hosted
4. Update roster metadata (stars, versions, pricing) for all 86 tools
5. Investigate and fix SIFT-128 synthetic dataset ground truth or HNSW parameter issue
6. Publish updated edition with full benchmark results

## License

Content is licensed CC BY 4.0 — share and adapt with attribution to **ArdurAI / Vector Databases & RAG Infrastructure Almanac**.
